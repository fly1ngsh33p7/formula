{
    "name": "formula",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs'); fs.copyFileSync('src/index.html', 'dist/index.html'); fs.copyFileSync('src/index.css', 'dist/index.css');\"",
        "watch": "tsc -w -p tsconfig.json"
    },
    "devDependencies": {
        "typescript": "^5.8.3"
    }
}
